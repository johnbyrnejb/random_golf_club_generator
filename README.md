# README

A python project to randomly select a glof club to play whilst out with pals. 

### Technology

* Python 3 project
* Fast API

# To Do
* Document the set up for requirements
* set up the webpage

### Run project
Open a Terminal and run the following command:

```uvicorn main:app --reload```